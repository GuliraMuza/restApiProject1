package peaksoft.service.impl;

public class StudentServiceImpl {
    /*@Service
public class GroupService {

    @Autowired
    private GroupRepository groupRepository;

    public List<Student> filterStudentsByTrainingFormat(TrainingFormat trainingFormat) {
        List<Student> filteredStudents = new ArrayList<>();

        List<Group> groups = groupRepository.findAll();
        for (Group group : groups) {
            for (Student student : group.getStudents()) {
                if (trainingFormat == TrainingFormat.ONLINE && student.isOnline()) {
                    filteredStudents.add(student);
                } else if (trainingFormat == TrainingFormat.OFFLINE && !student.isOnline()) {
                    filteredStudents.add(student);
                }
            }
        }

        return filteredStudents;
    }

    // Other methods and dependencies for the GroupService class
}
*/
}
